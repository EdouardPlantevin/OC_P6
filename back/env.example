# Configuration de la base de données
# Définir l'URL de connexion à MySQL (createDatabaseIfNotExist=true crée automatiquement la BDD)
DB_URL=jdbc:mysql://localhost:3306/mdd_db?createDatabaseIfNotExist=true

# Nom d'utilisateur MySQL
DB_USERNAME=root

# Mot de passe MySQL
DB_PASSWORD=votre_mot_de_passe_mysql

# Durée de validité du token JWT en secondes (86400 secondes = 24 heures)
JWT_SECOND=86400

# Clé secrète JWT en Base64 (générer avec: openssl rand -base64 64)
JWT_BASE64_SECRET=VotreCleSecreteBase64EncorePlusLonguePourLaSecurite

# Instructions:
# 1. Copier ce fichier vers .env à la racine du dossier back/
# 2. Remplacer les valeurs par vos propres paramètres
# 3. Ne JAMAIS commiter le fichier .env dans le repository

